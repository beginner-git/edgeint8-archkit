# ===========================================================================
# EdgeINT8-ArchKit 依赖清单
# 基于用户已有 CosCNN 环境 (torch==2.4.0 + CUDA GPU) 对齐版本
# 安装方式：在已有 conda 环境中执行
#   pip install -r requirements.txt
# 只会补装当前环境缺少的包，不会降级已有包
# ===========================================================================

# ---------- PyTorch 系列 (与 CosCNN 环境一致) ----------
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0

# ---------- ONNX 系列 (本项目核心) ----------
onnx>=1.14,<1.17
onnxruntime>=1.16,<1.20
# 如果要用 GPU 做 ORT 推理，可改用:
# onnxruntime-gpu>=1.16,<1.20

# ---------- 科学计算 (与 CosCNN 环境一致) ----------
numpy==1.24.3
scipy==1.10.1
pandas==2.0.3
scikit-learn==1.3.0

# ---------- 可视化 (与 CosCNN 环境一致) ----------
matplotlib==3.7.2
seaborn==0.13.2
pillow==10.4.0

# ---------- 工具包 ----------
tqdm==4.66.5
tabulate>=0.9
PyYAML==6.0.2
